<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">

</head>

<body>
    <main>



        <section>
            <ul>
                <% if(results.length) {%>
                    <% results.forEach(data=>{%>
                        <li>
                            <h4>
                                <%= data.title %>
                            </h4>
                            <p>
                                <%= data.contact %>
                            </p>
                            <p>
                                <%= data.status %>
                            </p>
                            <a href="/books/<%= data.id%>" target="_blank" rel="no opener no reference">view Book details</a>
                        </li>
                        <%}); %>
                            <% } else {%>
                                <p>data not available</p>
                                <%}%>
            </ul>
        </section>




        <div class="container4"></div>
    </main>
</body>

</html>